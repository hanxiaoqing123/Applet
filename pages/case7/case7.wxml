<view  class="page ">
	<view wx:for="{{icons}}"  class="ico">
       {{item.type}}==><icon  type="{{item.type}}"  size="{{item.size}}" />
	</view>
    <button   style="width:150px" >
      <icon  type="download"  class="btn"/>
    </button>
</view>

